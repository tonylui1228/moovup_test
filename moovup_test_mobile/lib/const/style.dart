import 'package:flutter/cupertino.dart';

const defaultPadding = 16.0;
const defaultAllEdgePadding = EdgeInsets.all(defaultPadding);
const defaultLeftRightPadding = EdgeInsets.fromLTRB(defaultPadding, 0, defaultPadding, 0);

const userIconSize = 80.0;